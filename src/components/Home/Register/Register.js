import React from 'react';
import Login from '../Login/Login';

const Register = () => {
    return (
        <div className="my-5">
            <Login></Login>
        </div>
    );
};

export default Register;